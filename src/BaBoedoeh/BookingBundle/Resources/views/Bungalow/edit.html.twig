{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Bungalow edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('bungalow') }}" class="btn">
            <span>Back to the list</span>
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
